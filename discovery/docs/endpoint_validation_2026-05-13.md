# Endpoint Validation 2026-05-13

This document records read-only validation results from the discovery sandbox. It does not promote any endpoint into production.

## Confirmed With Real Rows

### 清仓管理

- Route: `marketing.clearanceStockIndex`
- Endpoint: `POST /marketing/getAllClearanceStock`
- Validation file: `discovery/output/endpoint_validation_marketing_clearanceStockIndex_2026-05-13.json`
- Result: `code=200`, `rows=20`, total count observed in response: `145257`
- Cross-check:
  - Page name is 清仓管理.
  - Response fields include `sku`, `asin`, `salesChannel`, `seller_num`, `developer_num`, `applicant_amount`, `remain_stock`, `fulFillable`, `reserved`, `inbound`, `clearance_type`, payment/cost fields.
  - These fields match clearance/liquidation operations rather than generic page metadata.
- Status: `B_probable_data_source`

### 异常瑕疵库存

- Route: `pm.abnormalDefectiveInventory.index`
- Endpoint: `POST /pm/abnormalDefectiveInventory/getList`
- Validation file: `discovery/output/endpoint_validation_pm_abnormalDefectiveInventory_index_2026-05-13.json`
- Result: `code=200`, `rows=20`
- Cross-check:
  - Page script calls the same endpoint from the search action.
  - Response fields include `sku`, `asin`, `fnsku`, `defective_quantity`, `cur_defective_quantity`, `qty_3`, `qty_7`, `qty_30`, `fulFillable`, `reserved`, `inbound`, `inv_age_181_270`, `inv_age_271_365`, `inv_age_365_plus`, `progress_status`.
  - Page table headers include SKU/ASIN/FNSKU, defective quantity, sales quantities, stock state, and processing progress.
- Status: `B_probable_data_source`

### 销售核心数据

- Route: `product_line.sellerCoreData`
- Endpoint: `POST /pm/sale/getBySeller`
- Validation file: `discovery/output/endpoint_validation_product_line_sellerCoreData_2026-05-13.json`
- Result: `code=200`, `rows=1`
- Required parameter found from page script: `time=30`
- Cross-check:
  - Page script defines the same endpoint as `getTableDataUrl`.
  - Page columns include sales, gross profit, net profit, refund rate, ad spend, ad cost ratio, SP, ACOS, CPC, CPS, ROAS.
  - Response fields include `order_sales`, `gross_profit`, `net_profit`, `refund_percent`, `adv_spend`, `advCost`, `SP`, `ACOS`, `CPC`, `CPS`, `ROAS`.
- Status: `B_probable_data_source`

### 商品搜索表现

- Route: `searchPerformance.productIndex`
- Endpoint: `GET /searchPerformance/findProductSearchPerformance`
- Validation file: `discovery/output/endpoint_validation_searchPerformance_productIndex_2026-05-13.json`
- Result: `code=200`, `rows=20`, total count observed in response: `26176`
- Cross-check:
  - Page name is 商品搜索表现.
  - Response fields include `report_date`, `start_date`, `report_type`, `account_manager`, `account_nick_name`, `site`, `category`, `asin`, impression/click/cart-add/purchase metrics.
  - Metric groups are consistent with Amazon Search Catalog Performance reports.
- Status: `B_probable_data_source`

### 商品搜索表现导入文件

- Route: `searchPerformance.productIndex`
- Endpoint: `GET /searchPerformance/fileSearchPerformance`
- Validation file: `discovery/output/endpoint_validation_searchPerformance_productIndex_2026-05-13.json`
- Result: `code=200`, `rows=50`, total count observed in response: `105000`
- Cross-check:
  - Response fields include `page_name`, `import_file`, `import_user`, `status`, `created_at`, `updated_at`.
  - File names reference search catalog performance CSV imports.
- Status: `B_probable_supporting_source`

### 排名销量工具

- Route: `sales_ranking.index`
- Endpoint: `GET /pm/formal/salesRankingSearch`
- Validation file: `discovery/output/endpoint_validation_sales_ranking_index_2026-05-13.json`
- Validation params: `site=Amazon.com`, `type=Home & Kitchen`, `ranking=100`
- Result: `code=200`, `rows=2`
- Cross-check:
  - Page script requires `site`, `type`, and `ranking`, then renders `rank` and `sales`.
  - Response rows contain `rank` and `sales`.
- Status: `B_probable_data_source`

## Validated As Helper Sources Only

These returned JSON but no business table rows in the current validation. They look useful for filters or dimension mapping:

- `GET /administrator/developer/getSpecialTypeAll`
- `GET /commonUtil/getInfoFromRedisByKey`
- `GET /pm/formal/get_amazon_user_have_accounts`
- `GET /pm/authentication/getQuerySelect`
- `GET /internalControl/get_internal_audit_group`

## Not Confirmed Yet

### 产品成功率

- Route: `product_line.sellerSuccess`
- Candidate endpoint: `POST /pm/product/sellerSuccess`
- Page script evidence:
  - The page renders a table from this endpoint.
  - Columns include `seller_num`, `total`, `success`, `failure`, `inspect`, and rate fields.
- Validation result:
  - Without full form keys: `code=500`, missing `seller`.
  - After adding known blank form keys: still missing additional keys, then one attempt returned `404`.
- Current status: `C_unconfirmed_candidate`
- Next step:
  - Capture the exact form payload generated by the page's own query action, or serialize `#success_product_list` in browser context before request.

### 商品搜索表现站点数组

- Route: `searchPerformance.productIndex`
- Candidate endpoint: `GET /searchPerformance/getProductDataArray`
- Validation result: `404` with `site=Amazon.com`
- Current status: `C_unconfirmed_candidate`

## Current Count

- Confirmed data endpoints with real rows: 5
- Confirmed supporting endpoint with real rows: 1
- Confirmed helper/dimension endpoints: 5
- Unconfirmed candidates kept out of production: 2

## Promotion Note

All confirmed items are still `B_probable`, not `A_confirmed`, because operator confirmation and fixtures are still required before production migration.
